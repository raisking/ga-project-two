<div class="jumbotron jumbotron-fluid" id="front-page">
        <div class="container">
                <div class="row">
        <div class="col-md-3" >
                    <img src="/images/icon.jpg" class="img-responsive" id="logo" alt="logo">
                </div>
                <div class="col-md-6" id="center-div">
                    <h3>{{user.name}}'s Diet Plan</h3>
                </div>
            </div>
            <hr>
            <div class="container shopfood">
                {{#each user.foods}}
                <table class="table">
                    <thead class="thead-inverse">
                        <tr> <th>Day</th>
                            <th>Breakfast</th>
                            <th>lunch</th>
                            <th>Dinner</th>
                        </tr>
                    </thead>
                <tbody>
                    <tbody>
                        <tr>
                            <th>{{this.day}}</th>
                            <th>{{this.breakfast}}</th>
                            <th>{{this.lunch}}</th>
                            <th>{{this.dinner}}</th>
                        </tr>
                     </tbody>
                 </tbody>
                </table>
                <a href="/users/{{../user._id}}/foods/{{this._id}}">
            <button type="button" class="btn btn-success">
                       <span class="glyphicon glyphicon glyphicon-zoom-in">
              </span>
            View</button></a> {{/each}}
            </div><br>
            <div class="row">
                <div class="col-md-10">
                    <a href="/users/{{user._id}}/foods/new">
                    <button type="button" class="btn btn-primary">
                        <span class="glyphicon glyphicon glyphicon-plus">
              </span>
                    Create a New food</button></a>
                {{!-- </div> --}}
                {{!-- </div> --}}
                {{!-- <div class="row"> --}}
                {{!-- <div class="col-md-3"> --}}
                    <a href="/users">
                <button type="button" class="btn btn-primary">
                <span class="glyphicon glyphicon glyphicon-backward"></span>
                Back </button></a>
                </div>
            </div>
        </div>
    </div>
</div>